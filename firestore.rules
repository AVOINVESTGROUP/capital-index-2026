rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Locked baseline. Server-side workers should use IAM / Admin SDK.
    // Client access must be explicitly designed before any public app uses Firestore.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
